const config = {
    api:{
        // URL: 'http://localhost:10000/api/clientes' // Cambia la URL para que incluya la ruta completa al endpoint de tu API
        URL: 'https://clientesbackend.onrender.com/api/clientes'
    }
};

export default config;